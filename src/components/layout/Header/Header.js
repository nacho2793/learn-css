import React, { Component } from 'react';
import './Header.scss';

class Header extends Component {
  render() {
    return (
      <header className="learn-css-header">
        <span className="nextiva-logo">nextiva</span>
      </header>  
    );
  }
}

export default Header;
